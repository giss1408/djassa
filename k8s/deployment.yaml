apiVersion: apps/v1
kind: Deployment
metadata:
  name: djassa-api
  namespace: djassa
spec:
  replicas: 2
  selector:
    matchLabels:
      app: djassa-api
  template:
    metadata:
      labels:
        app: djassa-api
    spec:
      containers:
        - name: api
          image: registry.example.com/djassa/api:latest
          ports:
            - containerPort: 8000
          envFrom:
            - secretRef:
                name: djassa-secrets
          readinessProbe:
            httpGet:
              path: /ready
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 20
      securityContext:
        runAsUser: 1000
        runAsNonRoot: true
