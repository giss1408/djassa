Alternatives and next steps for the backend skeleton:

- Add SQLAlchemy models and alembic migrations.
- Add authentication (OAuth2 / JWT) and user model.
- Add docker-compose dev overrides and Postgres test container config.
